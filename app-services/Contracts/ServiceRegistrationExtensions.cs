namespace app_services.Contracts;

public static class ServiceRegistrationNotes
{
    public const string RegistrationTodo =
        "Register database-backed implementations for the Contracts interfaces " +
        "once data integration begins. Keep Razor pages bound to contracts so the " +
        "current mock UI can switch data sources with minimal UI rewiring.";
}
